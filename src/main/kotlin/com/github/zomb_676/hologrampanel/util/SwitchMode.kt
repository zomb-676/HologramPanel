package com.github.zomb_676.hologrampanel.util

enum class SwitchMode {
    /**
     * state is only true while pressing
     */
    BY_PRESS,

    /**
     * one press and release cycle will switch the state
     */
    BY_CYCLE
}