# Feature Update
- add more style configuration
- add a key to display hidden hologram, by default `ctrl`
- CycleSelector now configures ping screen/world, by default `u`.
  Also reduce keyboard conflict
- add Hologram Projector, which is intended to support hologram
  ping persistence and share. Also improve multiplayer experience.
  Right-click that block can quickly select
- ping player support move
- support adjust by rotation
- support local/player/world as transform orientation
- tooltip displayed for item can limit its height via the config file

# Bug Fixes
- fix repeat ping
- fix hologram not rendered on macOS
- fix CycleSelector not rendered correctly sometimes